[pytest]
# Add the project root to sys.path so test files can import
# top-level modules like `storage`, `signals`, `scoring`, etc.
# without needing relative imports or manual sys.path hacks.
pythonpath = .
testpaths = tests
